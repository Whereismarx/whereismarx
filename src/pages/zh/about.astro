---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { useTranslations } from "../../i18n/ui";

const t = useTranslations("zh");
---

<BaseLayout lang="zh" title={t("about")}>
  <section class="page-head">
    <h1>{t("about")}</h1>
    <p>{t("aboutIntro")}</p>
  </section>

  <section class="home-hero">
    <div class="home-copy-wrap">
      <div class="home-copy">
        <p>
          尽管大部分时间都在睡觉，在Marx醒着的时候，他会读很多书和杂志。Marx喜欢歌剧、话剧和当代艺术，你可以在博物馆和歌剧院找到他。如果你经常网上冲浪，你也有可能不小心在互联网的最深处发现他。Marx对民俗文化、恐怖电影和互动式游戏有特殊的兴趣。
        </p>

        <p>
          Marx热衷于观察这个世界，以及探究人的精神空间。虽然有商业、法律和佛学的教育背景，但Marx最感兴趣的还是那些难以被言说的微妙话题。
        </p>

        <p>
          Marx的名字是Marcus的变体，来自于罗马神话中的战神马尔斯，没有什么特殊政治含义。如果非要说有的话，Marx的政治倾向是：全自动奢华LGBTQIA+太空共产主义。
        </p>

      </div>
    </div>

    <div class="home-media">
      <div class="image-frame">
        <img src="/images/About.jpg" alt="About image" />
      </div>
    </div>
  </section>

  <style>
    .home-head {
      margin-bottom: 32px;
    }

    .home-hero {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(320px, 440px);
      align-items: start;
      gap: 40px;
    }

    .home-copy-wrap {
      min-width: 0;
    }

    .home-copy {
      max-width: 460px;
      font-size: 1.05rem;
    }

    .hero-actions {
      display: flex;
      gap: 14px;
      flex-wrap: wrap;
      margin-top: 28px;
    }


    .home-media {
      width: 100%;
      justify-self: end;
    }

    .image-frame {
      width: 100%;
      max-width: 440px;
      aspect-ratio: 4 / 5;
      border-radius: 28px;
      overflow: hidden;
      box-shadow: 0 20px 50px rgba(15, 23, 42, 0.12);
      background: rgba(255, 255, 255, 0.6);
    }

    .image-frame img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    @media (max-width: 840px) {
      .home-hero {
        grid-template-columns: 1fr;
        gap: 28px;
      }

      .home-media {
        order: -1;
        justify-self: start;
        width: 100%;
      }

      .image-frame {
        max-width: 100%;
        aspect-ratio: 16 / 10;
      }

      .home-copy {
        max-width: 100%;
      }
    }
  </style>
</BaseLayout>
