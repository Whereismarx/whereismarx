---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { useTranslations } from "../../i18n/ui";

const t = useTranslations("zh");
---

<BaseLayout lang="zh" title={t("siteTitle")}>
  <section class="page-head home-head">
    <h1>{t("homeTitle")}</h1>
    <p>{t("homeIntro")}</p>
  </section>

  <section class="home-hero">
    <div class="home-copy-wrap">
      <div class="home-copy">
        <p>
        Marx关心很多事情：进步主义技术伦理如何塑造人们的信念？资本主义逻辑如何在微观上组织人们的选择？有哪些习以为常但其实很奇怪的事情？有哪些看似毫无关系但其实紧密相连的事情？剥削压迫真的不正当吗？平等多元真的合理吗？活着总是优先答案吗？当这些问题的存在本身被一种温和放逐，这片自留地会提供一个小小的篝火营地。
        </p>

        <p>
        时代像暴风一样燃烧着。我们生活在一个高度繁华的消费景观园林之中，同时也生活在一个宏大叙事坍塌的废墟之上。我们活在不连续的瞬间串联成的状态里，活在情感与现实擦肩而过的片段间。
        </p>

        <p>
        明天会发生什么呢？Marx没有放弃对问题答案的追寻。
        </p>

        <div class="hero-actions">
          <a class="primary-btn" href="/zh/blog">查看文章</a>
          <a class="secondary-btn" href="/zh/about">关于Marx</a>
        </div>
      </div>
    </div>

    <div class="home-media">
      <div class="image-frame">
        <img src="/images/home-hero.jpg" alt="首頁主視覺圖片" />
      </div>
    </div>
  </section>

  <style>
    .home-head {
      margin-bottom: 32px;
    }

    .home-hero {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(320px, 440px);
      align-items: start;
      gap: 40px;
    }

    .home-copy-wrap {
      min-width: 0;
    }

    .home-copy {
      max-width: 460px;
      font-size: 1.05rem;
    }

    .hero-actions {
      display: flex;
      gap: 14px;
      flex-wrap: wrap;
      margin-top: 28px;
    }

    .primary-btn,
    .secondary-btn {
      display: inline-block;
      padding: 12px 18px;
      border-radius: 999px;
      font-weight: 600;
      text-decoration: none;
    }

    .primary-btn {
      background: #111827;
      color: white;
    }

    .primary-btn:hover {
      background: #1f2937;
      color: white;
    }

    .secondary-btn {
      border: 1px solid rgba(17, 24, 39, 0.1);
      background: rgba(255, 255, 255, 0.7);
      color: #111827;
    }

    .secondary-btn:hover {
      background: white;
      color: #111827;
    }

    .home-media {
      width: 100%;
      justify-self: end;
    }

    .image-frame {
      width: 100%;
      max-width: 440px;
      aspect-ratio: 4 / 5;
      border-radius: 28px;
      overflow: hidden;
      box-shadow: 0 20px 50px rgba(15, 23, 42, 0.12);
      background: rgba(255, 255, 255, 0.6);
    }

    .image-frame img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    @media (max-width: 840px) {
      .home-hero {
        grid-template-columns: 1fr;
        gap: 28px;
      }

      .home-media {
        order: -1;
        justify-self: start;
        width: 100%;
      }

      .image-frame {
        max-width: 100%;
        aspect-ratio: 16 / 10;
      }

      .home-copy {
        max-width: 100%;
      }
    }
  </style>
</BaseLayout>
